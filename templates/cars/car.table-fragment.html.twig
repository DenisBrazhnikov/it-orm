<tr class="{{ index % 2 ? 'bg-white' : 'bg-dark text-white' }} {{ index !=1 ?: 'text-danger'}}">
    <th scope="row">{{ index }}</th>
    <td>{{ car.name }}</td>
    <td>{{ car.date|date("m/d/Y") }}</td>
    <td>{% if car.editable %}<a href="/">Edit</a>{% endif %}</td>
</tr>